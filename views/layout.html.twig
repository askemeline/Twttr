<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block page_title %}TWEETER{% endblock %}</title>
    <link rel="stylesheet" href="assets/style.css">
</head>
<body>
{% block nav %}

    {% if session['u_id'] is not defined %}
        <div class="nav-bar">
            <img id="logo" src="http://icons.iconarchive.com/icons/designbolts/minimalist-social/256/Twitter-2-icon.png"
                 alt="">
            <a class="right register" href="?action=register">Sign up</a>
            <a class="right login" href="?action=login">Log in</a>
        </div>
    {% else %}
        <div class="nav-bar">
            <a href="?action=home">
                <img id="logo"
                     src="http://icons.iconarchive.com/icons/designbolts/minimalist-social/256/Twitter-2-icon.png"
                     alt="">
            </a>
            {#<a class="right register" href="?action=home">Home</a>#}
            <a class="right register" href="?action=tweet">TWEET</a>
            <a class="right register" href="?action=profileUser">Profile</a>
            <a class="right register" href="?action=logout">Log out</a>
        </div>
    {% endif %}


{% endblock %}
{% block errorblock %}
    {% if errors is defined %}
        <p>{{ errors }}</p>
    {% endif %}
{% endblock %}
{% block content %}
    {% if errors is defined %}
        <p>{{ errors }}</p>
    {% endif %}
{% endblock %}

<div class="footer">
    <br>{% block footer %} &copy; 2018 {% endblock %}
</div>
</body>
</html>