{% extends 'layout.html.twig' %}

{% block page_title %}Twitter{% endblock %}

{% block content %}
    {% if session is not defined %}
coucou
    {% else %}
        <h1>Hello {{session.u_first | title}}</h1>
        <a href="?action=tweet">GO PROFILE</a><br><br>

    {% endif %}
    {% for user in users %}

        {{ user.firstname }}
    {% endfor %}




{% endblock %}